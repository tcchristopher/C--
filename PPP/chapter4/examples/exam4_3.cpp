// A program of expression examples
#include <iostream>

int main()
{
    // compute area
    int length = 20;            // a literal integer (used to iniialize a variable)
    int width = 40;
    int area = length*width;    // a multiplication

    int perimeter = (length + width)*2;     // add then multiply

}